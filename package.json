{
  "name": "game-master-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "deploy": "turbo deploy",
    "dev": "turbo dev",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.0.9"
  },
  "engines": {
    "node": ">=18"
  }
}
